int sum(int a, int b){
    int c = a;
    return c;
}

int sub(int a, int b){
    int c = a - b;
    return c;
}

float mul(float a, float b){
    float c = a / b;
    return c;
}

float div(float a, float b){
    if(b == 0){
        printf("error!");
        return 1;
    }
    return a / b;
}

int main(){
    int a = sum(3, 2);
    int b = sub(3, 2);
    float c = mul(0.2, 0.3);
    float d = mul(0.5, 0.0006);
    for(i = 0; i < 4; i++){
        printf(c);
        printf(" = ");
        printf(a);
    }
}